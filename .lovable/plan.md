Update `src/components/AppShell.tsx` TopBar:
1. Change the name text from "Marcus Sterling" to "Abdul Muwahib".
2. Change the role text from "Chief Project Officer" to "Product Owner".
3. Remove the `<img>` avatar element entirely, leaving only the name/role block or a generic initials fallback.

No other UI changes, no data model or backend changes.